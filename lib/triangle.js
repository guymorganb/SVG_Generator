/**
 * JavaScript
 * Create a Class that can be used to generate a triangle .svg
 */

const shapes = require ('shapes');

class triangle extends shapes {
    consrtuctor(height, width, text){
        super(height, width, text);
        
    }
}


module.exports = triangle;